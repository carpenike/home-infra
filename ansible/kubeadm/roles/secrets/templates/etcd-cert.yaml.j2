apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: etcd-cert
  namespace: kube-system
data:
  ca: "{{ slurped_etcd_ca_file.content }}"
  cert: "{{ slurped_etcd_cert_file.content }}"
  cert_key: "{{ slurped_etcd_cert_key_file.content }}"
